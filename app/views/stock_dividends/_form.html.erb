<%= form_with(model: stock_dividend, local: true) do |form| %>
    <% if stock_dividend.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(stock_dividend.errors.count, "error") %> prohibited this stock_dividend from being saved:</h2>
            <ul>
                <% stock_dividend.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>
    <div class="field">
        <%= form.label :company_id %>
        <%= form.text_field :company_id %>
    </div>
    <div class="field">
        <%= form.label :amount %>
        <%= form.number_field :amount %>
    </div>
    <div class="field">
        <%= form.label :pay_date %>
        <%= form.date_select :pay_date %>
    </div>
    <div class="field">
        <%= form.label :ex_date %>
        <%= form.date_select :ex_date %>
    </div>
    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>
