<%= form_with(model: company, local: true) do |form| %>
    <% if company.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>
            <ul>
                <% company.errors.full_messages.each do |message| %>
                    <li><%= message %></li>
                <% end %>
            </ul>
        </div>
    <% end %>
    <div class="field">
        <%= form.label :ticker %>
        <%= form.text_field :ticker %>
    </div>
    <div class="field">
        <%= form.label :industry %>
        <%= form.text_field :industry %>
    </div>
    <div class="field">
        <%= form.label :inactive %>
        <%= form.check_box :inactive %>
    </div>
    <div class="field">
        <%= form.label :pse_company_id %>
        <%= form.text_field :pse_company_id %>
    </div>
    <div class="field">
        <%= form.label :pse_security_id %>
        <%= form.text_field :pse_security_id %>
    </div>
    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>
