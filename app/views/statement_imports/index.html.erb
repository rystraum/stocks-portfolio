<%= render "shared/header", text: "BPI Statement Imports" do %>
  <%= link_to "Import BPI Statement", new_statement_import_path, class: "btn btn-success" %>
<% end %>

<table class="table table-striped m-6">
  <thead class="thead-dark">
    <tr>
      <th>Date</th>
      <th>Filename</th>
      <th>Period</th>
      <th>Status</th>
      <th>Items</th>
      <th class="action-column">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @imports.each do |import| %>
      <tr>
        <td><%= import.created_at.strftime("%Y-%m-%d %H:%M") %></td>
        <td><%= import.filename %></td>
        <td><%= import.statement_period.presence || "-" %></td>
        <td><span class="badge <%= import.completed? ? 'badge-soft-success' : import.reviewing? ? 'badge-soft-warning' : 'badge-soft-danger' %>"><%= import.status.titleize %></span></td>
        <td><%= import.statement_import_items.count %></td>
        <td class="text-right">
          <%= link_to "View", statement_import_path(import), class: "btn btn-sm btn-primary" %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
