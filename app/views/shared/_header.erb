<header class="bg-white shadow mb-4">
    <div class="flex mx-auto py-6 px-4 sm:px-6 lg:px-8">
        <h1 class="text-3xl font-bold text-gray-900">
            <%= text %>
        </h1>
        <% if block_given? %>
            <div class="ml-auto">
                <%= yield %>
            </div>
        <% end %>
    </div>
</header>